replace: vlans {
{% for vlan in vlans %}
    {{ vlan.vlan_name }} {
        vlan-id {{ vlan.vlan_id }};
{% if 'Spine' in group_names %}
        l3-interface irb.{{ vlan.vlan_id }};
{% endif %}
    }
{% endfor  %}
}
interfaces {
{% for uplink in uplinks %}
    {{ uplink.uplink }} {
        unit 0 {
            family ethernet-switching {
                vlan {
replace:            members [{% for vlan in vlans %} {{ vlan.vlan_name }}{% endfor %} ];
                }
            }
        }
    }
{% endfor %}
{% for vlan in vlans %}
{% if 'Spine' in group_names %}
    irb {
replace: unit {{ vlan.vlan_id }} {
            family inet {
                address {{ vrrp.address }} {
                    vrrp-group {{ vrrp.group }} {
                        virtual-address {{ vrrp.vip }};
                        priority {{ vrrp.priority }};
                        preemept;
                        accept-data;
                    }
                }    
            }
        }
    }
{% endif %}
{% endfor %}
}
